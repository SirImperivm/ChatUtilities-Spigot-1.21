debug-mode: false

prefixes:
  default-prefix:
    prefix-string: ""
    color-code: "&7"
    placeholders-color: "&3"

chat-sounds:
  enabled: true
  sender-only: false
  sound: ui.button.click
  volume: 1.0
  pitch: 1.0

chat-item:
  enabled: true
  trigger-pattern: "[i]"

chat-mentions:
  enabled: true
  sound: entity.experience_orb.pickup
  volume: 1.0
  pitch: 1.0
  cooldown: 3 # Cooldown in seconds between mentions.

permissions:
  global-permission: chatutilities.*
  chat-formats:
    colored: chatutilities.chat.colored # This allows players to write normal colors in chat.
    special: chatutilities.chat.special # This allows players to use special characters to edit the format of a chat message.
  chat-item:
    use: chatutilities.chat-item.use
  chat-mentions:
    send: chatutilities.chat-mentions.send
    receive: chatutilities.chat-mentions.receive
    bypass-cooldown: chatutilities.chat-mentions.bypass-cooldown
  chat-utilities-command:
    main: chatutilities.command.main
    reload: chatutilities.command.reload
